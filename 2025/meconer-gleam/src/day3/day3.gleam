import gleam/list
import utils

pub fn day3p1(path) -> Int {
  let input = utils.get_input_lines(path)
  list.length(input)
}

pub fn day3p2(path) -> Int {
  let input = utils.get_input_lines(path)
  list.length(input)
}
